
```Bash
# A directory to work in
mkdir <your_project> && cd <your_project>
# Generate Azure Functions files
func init
# Select Option 4 (Python)
# Create a new function
func new
# Select Option 9 (HttpTrigger)
# Choose function name (e.g. chatbot)
# Create a Python virtual environment to work in
python –m venv .venv
# Open your project in Visual Studio Code
code .
# Start function
func start
```
